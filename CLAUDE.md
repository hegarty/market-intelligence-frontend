@AGENTS.md

# Session continuity

Read `SESSION.md` at the start of every session before doing anything else —
it's a living status doc (current implementation state, decisions/gotchas
from prior sessions, how to resume). Update it whenever you finish a
meaningful chunk of work or learn something a future session shouldn't have
to rediscover; keep entries current rather than letting them accumulate.
